# VM Manager 部署说明

## 项目简介
VM Manager 是一个用于管理 PVE 虚拟机和 Horizon 桌面的 Web 管理平台。

## 服务器信息
- **管理平台**: 192.168.11.10:5000
- **PVE 服务器**: 192.168.11.13:8006
- **Horizon 服务器**: 192.168.11.6

## 目录结构
```
vm_manager/
├── app.py              # 主应用入口
├── config.py           # 配置文件
├── requirements.txt    # Python依赖
├── api/
│   ├── horizon_api.py  # Horizon API客户端
│   └── pve_api.py      # PVE API客户端
├── auth/
│   └── ldap_auth.py    # LDAP认证模块
└── templates/
    ├── index.html      # 首页
    └── login.html      # 登录页
```

## 部署步骤

### 1. 环境要求
- Python 3.8+
- pip
- sshpass (用于自动化部署)

### 2. 安装依赖
```bash
cd /root/vm_manager
pip install -r requirements.txt
```

### 3. 配置
编辑 `config.py` 文件，修改以下配置：

```python
# PVE配置
PVE_CONFIG = {
    'host': '192.168.11.13',
    'port': 8006,
    'user': 'root@pam',
    'password': 'Gibbs001!',
    'verify_ssl': False
}

# Horizon配置
HORIZON_CONFIG = {
    'host': '192.168.11.6',
    'port': 443,
    'user': 'administrator',
    'password': 'Gibbs001!',
    'domain': 'archmond.ltd'
}

# LDAP配置
LDAP_CONFIG = {
    'server': 'ldap://192.168.11.6',
    'port': 389,
    'base_dn': 'DC=archmond,DC=ltd',
    'bind_dn': 'CN=Administrator,CN=Users,DC=archmond,DC=ltd',
    'bind_password': 'Gibbs001!'
}
```

### 4. 启动服务

**前台运行**：
```bash
cd /root/vm_manager
python3 app.py
```

**后台运行**：
```bash
cd /root/vm_manager
nohup python3 app.py > /tmp/vm_manager.log 2>&1 &
```

**查看日志**：
```bash
tail -f /tmp/vm_manager.log
```

**查看进程**：
```bash
ps aux | grep app.py | grep -v grep
```

**查看端口**：
```bash
lsof -i :5000
```

### 5. 访问
```
http://<服务器IP>:5000
```

默认账号：
- 用户名: wet21
- 密码: Gibbs001!

## systemd 服务配置（可选）

创建服务文件 `/etc/systemd/system/vm_manager.service`：

```ini
[Unit]
Description=VM Manager Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/vm_manager
ExecStart=/usr/bin/python3 /root/vm_manager/app.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

启用服务：
```bash
systemctl daemon-reload
systemctl enable vm_manager
systemctl start vm_manager
systemctl status vm_manager
```

## 常用操作

**重启服务**：
```bash
killall -9 python3
cd /root/vm_manager && nohup python3 app.py > /tmp/vm_manager.log 2>&1 &
```

**热更新代码后重启**：
```bash
killall -9 python3
sleep 1
cd /root/vm_manager && nohup python3 app.py > /tmp/vm_manager.log 2>&1 &
```

## PVE集群支持说明

当前代码使用硬编码节点名 `Cloud-03`。如需支持多节点PVE集群，需修改 `api/pve_api.py` 中的节点配置，将：
- `/api2/json/nodes/Cloud-03/qemu` 改为 `/api2/json/cluster/resources`

这样可以自动获取所有节点的虚机信息。

## 故障排查

1. **5000端口被占用**：
   ```bash
   lsof -i :5000
   kill -9 <PID>
   ```

2. **认证失败**：检查 config.py 中的密码配置

3. **PVE虚机获取失败**：检查 PVE 服务器连通性和节点名是否正确

4. **Horizon虚机获取失败**：检查 Horizon API路径是否正确（当前使用 /inventory/v1/machines）